# WordPress Plugin Updates — agent skill (workspace mode)

This folder is an agent skill for safely updating WordPress plugins over SSH.

For any request about WordPress plugin updates, connecting a site, pending
updates, or update reports: read `SKILL.md` and follow it exactly, starting
with its routing section. That includes setup questions ("connect my site",
"add a site"). Do not improvise plugin updates outside the SKILL.md process.
